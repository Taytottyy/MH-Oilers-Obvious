# Obvious-Hackathon — agent guidance

## Repository

- **Repo:** `Taytottyy/Obvious-Hackathon`
- **Default branch:** `main`
- **Provider:** GitHub (`gh` CLI available for PR work)

## Current status: empty repository

This repository is currently empty. Its entire contents are a single `README.md`
placeholder containing one heading (`# Obvious-Hackathon`). Specifically, there is:

- no application or library code;
- no dependency manifest (no `package.json`, `go.mod`, `Cargo.toml`,
  `pyproject.toml`, `Gemfile`, etc.);
- no `Dockerfile`, Compose file, or `Makefile`;
- no CI configuration and no `.env.example`;
- no guidance docs (`AGENTS.md`, `CONTRIBUTING.md`).

## How to work here

- **Do not assume a stack.** There is no runtime, package manager, framework,
  dev command, or port to reference — none are claimed in this file.
- **Do not scaffold an application or infer a framework.** Wait for the owners
  to commit code.
- This file will be replaced by a full contract (stack, commands, codebase map,
  local verification, snapshot info) by the **next setup run**, once the
  repository contains an application.

## Deliberate omissions from the scaffold run

- `.obvious/codebase-map.md` — not written; there is no code to map.
- `.obvious/skills/local-dev/SKILL.md` — not written; there is no local dev
  environment to record.

## Sandbox snapshot

- None captured (`dev_stack_healthy: false`). An empty repo has no dev stack to
  snapshot; snapshots are only taken on healthy dev stacks.

---
*Generated by the Obvious onboarding scaffold run on 2026-09-17 (UTC).*
