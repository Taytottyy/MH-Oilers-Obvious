# .obvious/config.yml — repo policy for Obvious agent runs.
# Auto-generated by the onboarding scaffold run on 2026-09-17 (UTC).

version: 1

# Branch that agent PRs target by default (auto-detected from GitHub).
defaultBranch: main

# Merge method for agent PRs: merge | squash | rebase.
# All three methods are currently enabled on this GitHub repo;
# squash is chosen as the default.
mergeMethod: squash

# When true, a human must approve and merge setup PRs;
# agents never merge their own PRs.
requireHumanMerge: true
